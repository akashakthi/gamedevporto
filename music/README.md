# Background Music

Put the exported Suno/background music file here as:

```text
BgmWeb.mp3
```

The website already includes a floating sound on/off button at the bottom-right corner.

If your file format is different, either rename/export it to `BgmWeb.mp3` or update the `<source>` paths in `index.html`.
